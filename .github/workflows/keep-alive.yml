name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Her 5 dakikada bir çalışır
  workflow_dispatch:       # Manuel tetikleme (istersen)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -s https://case-study-0hdw.onrender.com > /dev/null
